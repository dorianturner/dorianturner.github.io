function renderHarness(root) {
  root.innerHTML = `<div class="harness-architecture"><div class="architecture-row"><div class="architecture-node"><small>01 / interface</small><strong>Terminal UI</strong><span>Reads prompts and renders streamed events.</span></div><b aria-hidden="true">→</b><div class="architecture-node is-accent"><small>02 / transport</small><strong>EventBus</strong><span>Tokio broadcast channel carries typed Event variants.</span></div><b aria-hidden="true">→</b><div class="architecture-node"><small>03 / coordinator</small><strong>Session loop</strong><span>Owns active streams, tools, history, facts, and rollback state.</span></div></div><div class="architecture-branch"><div class="architecture-node"><small>04a / model boundary</small><strong>ModelProvider</strong><span>Provider adapters return a stream of Token, Usage, and Finished events.</span></div><div class="architecture-node"><small>04b / tool boundary</small><strong>Approval → tool runner</strong><span>RunCommand, WriteFile, ApplyPatch, and RollbackLastChange are explicit requests.</span></div></div><div class="context-band"><div><small>ContextEngine</small><strong>Build request</strong></div><span>recent history + compacted context + ConversationFacts + project references</span><b aria-hidden="true">→</b><div><small>ModelRequest</small><strong>bounded prompt</strong></div></div><p class="architecture-caption">The session is the coordination point: it keeps the recent conversation tail, folds older turns into compacted_context when the budget is exceeded, extracts durable facts, then assembles a bounded request through ContextEngine.</p></div>`;
}

window.projectWidgets = window.projectWidgets || {};
window.projectWidgets.harness = renderHarness;
